spring:
  datasource:
    url: jdbc:mysql://localhost:3306/niconicode?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root

logging:
  level:
    com.niconicode: debug
    org.springframework.security: debug
